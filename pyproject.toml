[project]
name = "autolab"
version = "0.1.0"
description = "Autoresearch integrated with LLM-wiki: autonomous AI research agents that search literature, run experiments, and build compounding knowledge"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
