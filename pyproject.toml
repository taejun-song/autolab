[project]
name = "autolab"
version = "0.1.0"
description = "Autonomous research agent on top of an LLM wiki"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
